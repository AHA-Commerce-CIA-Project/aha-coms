# Execution Plan: Monorepo Consolidation + Heroes Cleanup

> Last updated: 2026-05-14 (**CP18 CROSSED + FU-23 sealed + FU-24 filed.** Three operator-window steps walked end-to-end same-day after the T77+T78 commit landed: (1) `app_webhook_endpoints.status` flip from `disabled` → `active` on portal's DB via direct SQL UPDATE; (2) `taxonomy_cache` seed via SQL-to-SQL copy from portal's `org_taxonomies` (13 rows for `'teams'`) — skipped the `sync-taxonomies.ts` script entirely because the ADC impersonation path required `roles/iam.serviceAccountTokenCreator` on `coms-fast-web-sa` which `handers.the@ahacommerce.net` didn't carry pre-window (granted mid-session via `gcloud iam service-accounts add-iam-policy-binding` since the operator account has `roles/owner` and can self-grant); (3) end-to-end webhook delivery via `coms-portal-cli smoketest fast` (Spec 06 Rev 4 PR B) — `POST /api/v1/apps/fast/smoketest` dispatched an `app.smoketest` envelope to fast, fast's verifier 401'd on first attempt due to a `SELF_PUBLIC_URL`/audience mismatch (cloud-run.tf had it as `${fast_public_origin}/fast`, but portal mints `aud=new URL(endpoint.url).origin=https://aha-coms.web.app`, no path suffix), fixed live via `gcloud run services update --update-env-vars=SELF_PUBLIC_URL=https://aha-coms.web.app`, retry returned `{"ok":true,"endpoints":[{"status":200,"latencyMs":303}]}` and `portal_webhook_events` holds one dedup row for the test event. **CP18 closed end-to-end.** Two follow-up artefacts the operator window surfaced: **FU-23** (the prior gate-fix work; `deploy-fast.yml`'s `[skip-db-push]` whole-message substring match self-defeated commit `e76bbd6` because its body explained why the token wasn't being used — fixed by adding a `Resolve db-push skip token` step that reads only the first line of the commit message) and **FU-24** (heroes carries the same `SELF_PUBLIC_URL` audience-mismatch crack fast just mended; heroes' `SELF_PUBLIC_URL` is `https://coms-heroes-api-45tyczfska-et.a.run.app` while portal mints `aud=https://aha-coms.web.app` for heroes' single-origin endpoint too — currently latent because heroes' `app_webhook_endpoints.status='disabled'` from a 2026-05-12 "Cloud Tasks retries exhausted" failure, but the next re-enable will trip the same 401). IaC follow-up commit aligns `infra/fast/cloud-run.tf`'s `SELF_PUBLIC_URL` with the live env so the next deploy doesn't revert, plus an anchored comment block explaining the audience convention so the next reader has the lesson attached. **What stays open:** runbook step-order rewrite (light doc work; the cracks from this window's three steps would inform the rewrite too — at minimum the `SELF_PUBLIC_URL` audience check belongs in the runbook before CP18 steps); FU-22 (`spec07-register-fast.ts` drift-detect-and-upsert, now even higher leverage since the manual SQL UPDATE on `app_webhook_endpoints.status` would have been a one-line script invocation); FU-24 (heroes parallel crack); T75 closer (three more screenshots); T86 Lighthouse + on-device install (Phase 9 operator-led); Phase 10 (T87–T90) to seal CP21. **Previous status block below for the T77 + T78 commit:**)

> 2026-05-14 (**T77 + T78 SEALED code-side — fast's webhook consumer + taxonomy projection live in the tree.** Phase 7's code-side surface is now complete; only the operator-window flip of `app_webhook_endpoints.status` from `disabled` → `active` (portal-side) + the one-shot `bun run apps/fast/scripts/sync-taxonomies.ts` initial sync against fast's prod DB remain to close CP18 fully. **Twelve-piece slice landed as one commit:** (1) Prisma schema gains `PortalWebhookEvent` (eventId PK + receivedAt) and `TaxonomyCache` (composite PK `taxonomyId+key`, value, metadata Json, cachedAt) under a new "PORTAL BRIDGE" section; both additive so `prisma db push` applies on next deploy without `[skip-db-push]` gating. (2) `apps/fast/lib/portal/oidc.ts` — `verifyGoogleIdToken` mirroring `apps/heroes-api/src/lib/oidc.ts` (OAuth2Client singleton; throws on signature / expiry / unverified-email / SA-mismatch). (3) `lib/portal/unwrap-envelope.ts` — pure helper pinning the 2026-05-05 heroes regression (passing full envelope to handlers instead of envelope.payload) at the fast-side mirror. (4) `lib/portal/role-mapping.ts` — `mapPortalRoleToFastRole()` maps portal's `'employee'|'leader'|'admin'` to fast's `'member'|'leader'|'admin'` column convention (leader/admin passthrough, employee→member, unknowns→null so callers skip the update — defensive against future enum drift). (5) Seven per-event handlers in `lib/portal/handlers/` covering all 8 events fast subscribes to per spec07-register-fast.ts: user.provisioned (Prisma upsert keyed on portal_sub), user.updated (delta-aware on changedFields), user.offboarded (accountStatus='rejected'), app_config.updated (role-only — teamId deferred because fast's local Team UUIDs don't yet reconcile with portal's 'teams' taxonomy keys), employment.updated (no-op until fast denormalises HR fields), alias.updated (no-op; fast has no alias cache), taxonomy.upserted (per-row upsert into TaxonomyCache, metadata cast as Prisma.InputJsonValue), taxonomy.deleted (single deleteMany with `in` filter). (6) `lib/portal/dispatch.ts` — 8-event handler map + `dispatchPortalEvent(event, body)` that no-ops on unknown events. (7) `app/api/webhooks/portal/route.ts` — Next.js Route Handler that orders: missing X-Portal-Event / X-Portal-Event-Id → 400; missing PORTAL_SERVICE_ACCOUNT_EMAIL / SELF_PUBLIC_URL env → 500; missing/non-Bearer authorization → 401; failed ID-token verify → 401; dedup via `prisma.portalWebhookEvent.createMany({skipDuplicates: true})` — count===0 short-circuits 200; malformed envelope → 400; clean → dispatch → 200. Exports `force-dynamic` for the same reason as `/api/health` (DB roundtrip can't run during `next build`). Middleware T68's allowlist already covers `/api/webhooks` so no auth-cookie pre-check fires. (8) `lib/portal/portal-api-client.ts` — outbound client mirroring `apps/heroes-api/src/lib/portal-api-client.ts` (cached GoogleAuth + per-audience IdTokenClient map; `fetchTaxonomySync()` calls `GET /api/taxonomies/sync` against `PORTAL_BASE_URL`). The actual portal endpoint is `/api/taxonomies/sync` (returns all subscribed taxonomies in one round-trip via `requireAppToken()`) — not `/api/v1/taxonomies/:id/entries` as the original T78 task body imagined; corrected in the code + the todo entry. (9) `scripts/sync-taxonomies.ts` — operator-runnable one-shot that iterates `fetchTaxonomySync().taxonomies` and upserts every entry via the same code path the webhook uses, so re-runs are idempotent; runbook embedded as a header comment (impersonate `coms-fast-web-sa` via gcloud ADC, open Cloud SQL Auth Proxy on 5435, set `PORTAL_BASE_URL=https://aha-coms.web.app`, `bun run apps/fast/scripts/sync-taxonomies.ts`). (10) Tests: 21 cases across three files — `unwrap-envelope.test.ts` (5 cases mirroring heroes' envelope-contract pin), `role-mapping.test.ts` (5 cases), `route.test.ts` (11 cases covering header validation, OIDC env config, Bearer validation, dedup short-circuit, dispatch with payload-not-envelope check, malformed body, missing payload). All mocks via `mock.module('@/lib/db' | '@/lib/portal/oidc' | '@/lib/portal/dispatch', ...)` so the tests stay free of DB / Google / handler-side dependencies. `bun test ./apps/fast/lib/portal/*.test.ts ./apps/fast/app/api/webhooks/portal/route.test.ts` reports 21 pass / 0 fail / 53 expect() calls. Pre-existing fast tests (health + load-fast-auth-user) stay green. (11) `apps/fast/package.json` gains `google-auth-library: ^9.15.1` (matches heroes-api's version exactly) + `@coms-portal/sdk: workspace:*` for the `PortalWebhookEnvelope`, `UserProvisionedPayload`, `UserUpdatedPayload`, `UserOffboardedPayload`, `AppConfigEvent`, `TaxonomyUpsertedPayload`, `TaxonomyDeletedPayload` type imports. `bun install` resolved + extracted 39 new packages; lockfile updated. (12) `bun run --filter @coms-portal/fast typecheck` exits 0. **Net session outcome:** Phase 7's code-side surface complete. **What stays for the next operator window:** (a) `app_webhook_endpoints.status='disabled'` → `'active'` for fast's row in portal's DB (raw SQL today; FU-22 would let `spec07-register-fast.ts` handle it cleanly when it lands); (b) confirm `prisma db push` against fast's Cloud SQL applied the additive PortalWebhookEvent + TaxonomyCache tables on the next deploy (no [skip-db-push] gating); (c) one-shot `bun run apps/fast/scripts/sync-taxonomies.ts` against fast's prod DB to seed 'teams' entries; (d) trigger a portal-side test event + confirm the dedup row lands + the handler runs. After that, **CP18 crosses fully** and Phase 9 (T86 Lighthouse + install) + Phase 10 (T87–T90) close Spec 05 at CP21. **Carried-forward open items:** runbook step-order rewrite (light doc work touching `apps/fast/docs/runbook-cp14-cp20-window.md` only); FU-22 (`spec07-register-fast.ts` drift-detect-and-upsert per heroes' FU-1 shape, ~60 lines, naturally pairs with the next operator window's `status` flip); T75 closer (three more screenshots — desktop+dark, mobile+light, mobile+dark); PK promotion of `User.id → portal_sub` (intentionally deferred to its own commit + window per T64's body, 38 product-model FKs cascade); fast's dead state in TopNav (cosmetic cleanup commit, see Finding 4 below). **Previous status block below for the 2026-05-14 operator window:**)

> 2026-05-14 (operator window walked the runbook end-to-end + surfaced five findings + mended them same-session — **CP15 + CP17 CROSSED; CP14 destructive cut sealed (PK promotion still deferred); T70 + T74 + T76 + T84 + T85 sealed operator-verified.** The operator window walked the runbook end-to-end + surfaced five findings + mended them same-session, all within a single sitting after the runbook authoring. **Finding 1: `/api/auth/me` 500.** Cloud Run logs traced to Postgres 42P10 ("no unique or exclusion constraint matching the ON CONFLICT specification") — T60's `0001_add_portal_sub.sql` had applied a **partial unique index** (`WHERE portal_sub IS NOT NULL`) instead of a regular unique constraint. Prisma's `INSERT … ON CONFLICT (portal_sub) DO UPDATE` requires the conflict_target to match an index with no WHERE clause; partial indexes need the WHERE filter repeated in the conflict spec, which Prisma doesn't emit. The two forms are functionally equivalent for the data-integrity invariant (Postgres treats multiple NULL values as distinct in a regular UNIQUE constraint, allowing many NULL rows while enforcing uniqueness among non-NULL values), but ON-CONFLICT-matching ≠ data-integrity-equivalence. Hotfix: drop the partial index + add a regular unique constraint of the same name in a single transaction; zero data move because the existing uniqueness guarantee transfers identically. Committed at `a66b805` with the 0001 SQL file rewritten to mirror what `prisma db push` would emit from the Prisma schema's declarative `portal_sub String? @unique`. The "Why a regular UNIQUE constraint, not a partial unique index" preamble names the failure mode so the next migration author doesn't repeat it. **Finding 2: `/api/auth/me` 500 (second wave).** After the unique-key swap, upserts now reached ON CONFLICT but the INSERT clause itself failed with Postgres P2011: `Null constraint violation on the fields: (\`emailVerified\`)`. The deployed Prisma schema (post-T64) no longer declares `emailVerified`, but the live DB still carried the column as NOT NULL. Prisma's INSERT clause omitted the field; Postgres validates NOT NULL BEFORE evaluating ON CONFLICT, so even rows that would have hit the UPDATE branch aborted at INSERT-validation. The fix is exactly what `apps/fast/prisma/sql/0002_drop_better_auth_tables.sql` was authored for at T64. The runbook had it as step 5 (after the walk), but the walk couldn't reach success until the destructive cut landed — so the order swapped in execution. Pre-apply counts: `session: 70 rows`, `account: 12 rows`, `verification: 0 rows`, `user.emailVerified non-null: 13 values`. Post-apply verification: `to_regclass()` returns NULL for all three table names; `information_schema.columns` shows zero rows for `user.emailVerified`. Deploy-first-then-migrate ordering held: the deployed revision (commit `8935fcf`) carried the post-T64 schema reading nothing from the dropped surfaces. Sealed at commit `d70d53f`. **CP14's destructive predicate crossed**; PK promotion of `User.id → portal_sub` (cascading 38 product-model FK relations, or `User → fast_profiles` per integration contract §2) stays intentionally deferred to its own commit + window. **Finding 3: portal launcher → legacy run.app URL.** Clicking the FAST card from portal's dashboard sent the operator to `https://aha-fast-app-45tyczfska-et.a.run.app/login?portal_code=…` (the legacy `aha-fast-app` Cloud Run service from before T80) instead of `https://aha-coms.web.app/fast`. Root cause: `app_registry`'s fast row held the legacy URL from a pre-Spec-05 registration; T76's `spec07-register-fast.ts` is idempotent on `slug='fast'` (logs "already registered" + exits without UPDATE). Direct SQL UPDATE on `app_registry` set `url`, `broker_origin`, `service_account_email`, `health_check_url` to single-origin shape (`https://aha-coms.web.app/fast`, `https://aha-coms.web.app`, `coms-fast-web-sa@…`, `https://aha-coms.web.app/fast/api/health`); webhook endpoint URL updated to match. Portal dashboard's FAST card flipped from "Degraded" to healthy within 60 seconds (the dashboard probe picked up the new `health_check_url` and got 200 from `/fast/api/health`). **Follow-up worth filing:** teach `spec07-register-fast.ts` drift-detection-and-upsert per heroes' FU-1 shape, so the next operator window doesn't need raw SQL for an existing-row update. **Finding 4: visual duplicates in the dual-bar stack.** With the auth corridor live, the chrome rendered correctly + surfaced the visual cost of the hybrid mount — two theme toggles (one in ServiceBar's right slot, one in TopNav's right cluster), two profile avatars (HT in ServiceBar's AccountWidget, H in TopNav's avatar), two sign-out paths, name + role rendered twice. The duplicate-functionality stack felt crowded at 100px tall (36px ServiceBar + 64px TopNav). Strip pass at commit `61a58be`: removed TopNav's theme toggle button + the entire profile menu (avatar + name + role + dropdown with email + role + Profile Settings + Changelog + Sign out); kept module tabs + notifications bell + toast popups system (fast-specific surfaces with no chrome-contract equivalent). Profile Settings + Changelog moved to fast's Sidebar footer (Settings icon + Sparkles icon, both expanded + collapsed shapes) per per-app principle. Net: TopNav reads less crowded; only the bell remains in its right cluster; AccountWidget covers cross-app concerns alone. The accepted cost: TopNav's now-dead state (`showDropdown`, `userMenuRef`, `displayName`, `displayRole`, the destructured `signOut`, `useTheme()`'s `toggleTheme`, the outside-click handler, plus unused imports for Sun/Moon/Shield/User/LogOut/Sparkles) survives in the source — typecheck passes, build green, but a future commit can sweep. **Finding 5: Sidebar overlap with the dual-bar stack.** When the Sidebar expanded on hover, its w-64 panel overlapped TopNav's logo + first module tab on the left. Root cause: Sidebar's fixed positioning carried `top-16` (64px) from the pre-hybrid-mount shape, when only TopNav lived above and started at viewport y=0. After T72/T73 added ServiceBar (fixed top-0, 36px) and shifted TopNav to `sticky md:top-9` (occupies viewport [36, 100]), the Sidebar at top-16 = 64px collided with TopNav's sticky range in [64, 100] — a 36px overlap zone right where "AHA COMSS" + "AHA Fast" live. Fix at commit `c8c1acd`: shift Sidebar to `top-[100px]` + adjust height to `h-[calc(100vh-100px)]` (36 + 64 = 100 named in the comment block so the constant has provenance). Three vertical zones now stack cleanly: ServiceBar [0, 36] → TopNav [36, 100] → Sidebar [100, ...]. Zero overlap. **Net session outcome:** sixteen commits on `origin/main`, four checkpoints crossed (CP14 partial, CP15, CP17, CP19 + the prior CP18 partial via T76's URL fix), five findings mended, one runbook-execution-order lesson recorded. **What's still open in the runbook:** T75 screenshot grid (three more captures: desktop+dark, mobile+light, mobile+dark — desktop+light captured during the walk); T77 webhook consumer + T78 taxonomy projection (Phase 7 code-side); T86 Lighthouse audit + on-device install verification (Phase 9 operator-led); Phase 10 (T87 smoke + T88 perf + T89 fast README + T90 contract update) to seal CP21. **The runbook itself needs a step-order rewrite** in a follow-up — the executed order (T76 first → then destructive cut → then chrome cleanup → then walk) inverts the authored order; the authored "walk first, then register, then destructive" doesn't work because (a) walk needs registration for the user to have fast in `apps`, (b) walk needs destructive cut for `/api/auth/me` to return 200. The runbook commit at `7532182` should be updated to reflect this lesson — Step 1 deploy, Step 2 register-fast + URL fix, Step 3 destructive Prisma apply, Step 4 walk, Step 5 screenshots, Step 6 Lighthouse + install. **Next code-side pickup:** T77 webhook consumer at `apps/fast/app/api/webhooks/portal/route.ts` — the missing leg of CP18 (the `app_webhook_endpoints` row already exists with `status='disabled'`; T77 lands the consumer that flips it to `active`).

> 2026-05-14 (the chrome-mount authoring + runbook draft, earlier in the same day — **T72 + T73 + T74 + T84 + T85 SEALED code-side; operator runbook for the CP14 → CP20 window authored.** Five commits later in the same day the T83 seal landed, the code-side work that was waiting for CP19 (now crossed) walked four chunks: **T72 + T73** (commit `5c0a4e2`) adopted the hybrid chrome mount — adding `@coms-portal/ui-react` + `@coms-portal/account-widget-react` + `@coms-portal/design-tokens` as workspace deps, wiring the three `@import` lines into `apps/fast/app/globals.css` after the existing `tailwindcss` import (design-tokens for brand palette, ui-react/account-widget-react styles for Tailwind v4 `@source` registration), authoring `apps/fast/components/layout/SuiteServiceBar.tsx` as a thin client-component wrapper around `ServiceBar` with a static three-entry SUITE_SERVICES (portal / heroes / fast) and `useTheme()` wiring, mounting `<SuiteServiceBar />` above the existing `<TopNav />` in `apps/fast/components/AppShell.tsx`, and shifting TopNav's sticky offset from `top-0` to `top-0 md:top-9` so the 64px in-app bar sits below the 36px ServiceBar on desktop. The hybrid framing **replaced the plan body's original "delete fast's local AppShell + Sidebar + Header wholesale" shape** — AppShell.tsx isn't pure chrome (it orchestrates app-wide modals, the profile panel, the breadcrumb, layout reflow, public-route bypass), TopNav.tsx (657 lines) carries fast-specific notification + module-tab + toast systems the React chrome doesn't replicate, Sidebar.tsx (458 lines) is per-module contextual nav, and BottomNav.tsx (227 lines) has badge polling + a slide-up More sheet. The hybrid keeps fast's in-app surfaces intact and adds the cross-app cookie crumb at the top on desktop only — mobile stays untouched (ServiceBar is `hidden md:flex` by design). **T74** (commit `bab073b`) lifted the static SUITE_SERVICES to dynamic projection through the existing `/api/auth/me` round-trip: `FastSession` now carries `{user, appCatalog}` from `loadFastAuthUser` (the helper was already returning `result.appCatalog` and `require-fast-auth` was discarding it); `/api/auth/me/route.ts` includes `appCatalog: session.appCatalog` in its 200 response; `useAuth`'s context gains an exported `AppCatalogEntry` type + an `appCatalog: readonly AppCatalogEntry[]` field that fetchSession populates from the response payload alongside profile/loading; SuiteServiceBar's `services` + `appSwitcher` derive from appCatalog when populated and fall back to the static three-entry list during the first paint so the strip never looks empty. **AccountWidget mounts into ServiceBar's `right` slot** when user is non-null — currentApp="fast", portal origin from `NEXT_PUBLIC_PORTAL_ORIGIN` (fallback `aha-coms.web.app`), user projected from useAuth (name + email + portalRole + apps), appSwitcher matching the services list, sign-out flows through AccountWidget's own helper. **TopNav's in-app profile menu is deliberately kept** — it carries fast-specific destinations (Profile Settings, Changelog) that AccountWidget doesn't replicate; the two-avatar visual artifact on desktop is documented inline as the hybrid mount's intentional cost, with T75's parity review deciding whether a follow-up collapses them. **T84 + T85** (commit `c189e86`) anchored fast at the `/fast/` scope at the manifest + service-worker layers — the existing manifest and `sw.js` both predated T65's basePath flip and carried unprefixed paths. `apps/fast/app/manifest.ts` rewrote: `name` flipped from "AHA COMSS — Company Support Systems" to "AHA Fast"; `short_name` "Fast"; `start_url` + `scope` + `id` all `'/fast/'` (closes FU-10's scope-overlap collision at the three-PWA scale); `background_color` matches `theme_color` at `#0F0E7F` so standalone launch doesn't flash bright white; icon `src` paths carry the `/fast/` prefix explicitly because Next.js's metadata API doesn't auto-prefix manifest values. `apps/fast/public/sw.js` rewrote: cache name `aha-comss-v48` → `aha-fast-v1` (the rename surfaces the boundary cleanly + activate's prune list eats every prior cache key on next install); STATIC_ASSETS re-prefixed across five entries; API skip-guard flipped from `startsWith('/api/')` to `startsWith('/fast/api/')`; static-asset matchers + offline fallback all adjusted. `apps/fast/components/PWAInstaller.tsx`'s registration path flipped from `/sw.js` to `/fast/sw.js` (client-side fetch literals don't transit basePath). **Operator runbook** (commit `7532182`) authored at `apps/fast/docs/runbook-cp14-cp20-window.md` — a 410-line single document that walks six steps in one operator sitting and crosses **CP14 + CP17 + CP18 + CP20** in 30–60 minutes: (1) push + watch `deploy-fast.yml`; (2) T70 authenticated cross-app walk (sign-in → fast → ServiceBar tabs → AccountWidget popover → sign-out); (3) T75 screenshot grid at desktop+mobile × light+dark with comparison against heroes' T54 baseline; (4) T76 `spec07-register-fast.ts` with the post-Phase-4 URLs + flip the monitoring uptime alert from `enabled = false` to `enabled = true` via `tofu apply` in `infra/fast/`; (5) CP14 destructive Prisma apply (`prisma/sql/0002_drop_better_auth_tables.sql` via Cloud SQL proxy on port 5435, prerequisite check for `portal_sub` null count, verify-three-NULLs from `to_regclass`, deploy-first-then-migrate ordering holds because the current revision reads nothing from the to-be-dropped tables); (6) T86 Lighthouse PWA audit + on-device install of all three PWAs (portal, heroes, fast) on Chrome (Android) + Brave for the three-tiles-one-device proof. Each step has copy-pasteable shell, the `sed` patterns are lifted verbatim from `deploy-portal-api.yml:119` (Unix-socket DSN form) and `deploy-fast.yml:142` (public-IP DSN form) so the host-rewrite for two Cloud SQL proxies on different ports stays operationally tested. The runbook closes with a six-row "when things go sideways" recovery table covering deploy Prisma binary mismatch, sign-in loop, idempotent registration, psql schema search_path, manifest 404, scope-overlap install refusal. **Net code-side state of Spec 05 after this chunk:** every task that can be done from a CLI is now `[~]` or `[x]`. The remaining `[ ]` items (T70 sign-in walk, T75 screenshot capture, T76 registration, T77 + T78 webhook + taxonomy, T86 Lighthouse + install, CP14 destructive apply, CP17/CP18/CP20 checkpoint flips, and Phase 10 T87–T90) all gate on the next operator window. After that window, **CP21 (Spec 05 complete)** is one Phase 10 session away. **The operator pushes the five commits, follows the runbook, and the suite holds two production apps end-to-end on the contract.**

> 2026-05-14 (the T83-seal block — operator window complete; **T83 SEALED — Spec 05 Phase 8 operator-window complete; `coms-fast-web` Cloud Run service live; `/fast/api/health` returns 200 + `dbReachable:true`; CP19 crossed.** Twelve operator windows (Op-1 through Op-12) walked one session: state bucket precreate + `tofu init` + `tofu plan` surfaced four resource cracks (**Op-2** trimmed `infra/fast/`'s secret references from 7 to the 3 actually wired — Resend excluded per FU-18, Slack excluded per FU-17, cron moved to plaintext-via-tfvars, webhook HMAC dormant until T77; **Op-3** folded FU-15's prod DB password rotation alongside `aha-fast-db-url`'s Secret Manager refresh; **Op-4** corrected the uploads bucket default from the principle-2 name `coms-fast-uploads-prod` to the legacy live name `aha-fast-uploads-prod` per principle 1's recorded naming-wart artefact; **Op-5** the `tofu apply` proper — created the Cloud Run service + runtime SA + deployer SA + WIF pool + AR repo + monitoring uptime check (`enabled = false`) + iamcredentials API). **Op-6**'s deploy chain took seven workflow runs to reach green: path-filter isolation probe (`805ee0f`, no portal-* or heroes-* runs fired — proven), then six Dockerfile cracks in sequence — workspace resolution across all five Dockerfiles (`4723bf4`), the postinstall hook running inside the workspace (`3666880`), missing root `package.json` in builder (`83efe36`), Prisma binary-targets mismatch between Debian 12 builder and Debian 11 runtime (`43fbafd`), and finally the libssl1.1 / Debian Bullseye runtime pin (`bd34483`) — Prisma 5.22.0's runtime detection picks `debian-openssl-1.1.x` even on Bookworm hosts, so the runtime stage pins to `node:20-bullseye-slim`. **Op-7** (`84dab7c`) matched legacy `aha-fast-app`'s always-warm shape — Path X (`min_instance_count = 1`, `cpu_idle = false`), the deliberate fast-specific choice that contrasts with portal/heroes' `min=0` + uptime-probe-induced warmth Op-12 surfaced. **Op-8** registered the WIF repo vars (`WIF_PROVIDER_FAST` + `WIF_SA_FAST`) via `gh variable set` from the apply's outputs. **Op-10** (`4fc602d`) wired the destructive-migration escape hatch — the deploy workflow's `prisma db push` step now respects a `[skip-db-push]` token in commit messages that Op-6's chain used to avoid migrations during Dockerfile iteration; the same token will gate T64's eventual operator-driven destructive apply. **Op-11** verified the live corridor by curl: `https://aha-coms.web.app/fast/api/health` → 200 + `{"status":"ok","dbReachable":true,"timestamp":"…"}`; `https://aha-coms.web.app/fast/dashboard` (unauthed) → 307 → `https://aha-coms.web.app/portal?app=fast&redirect_to=%2Ffast%2Fdashboard` confirming middleware T68 fires for unauthed page requests with the correct cross-app redirect shape. **Op-12** (`d1770a0`) surfaced the structural finding that every Cloud Run service with a 5-minute uptime check is effectively always-warm regardless of `min=0` claims (Cloud Run's ~15-minute idle-shutdown timeout resets each probe); queued against FU-21's cost audit with the per-service decision space recorded inline. **Net code-side state:** T79 promotes `[~]` → `[x]` (live curl green; the portal-dashboard row stays operator-deferred until T76 lands `app_registry` registration so the dashboard has a consumer to flip). T83 promotes `[ ]` → `[x]` (Cloud Run live; WIF repo vars live; deploy workflow green; path-filter isolation proven). **CP19 crosses** on T83's success — `coms-fast-web` is live, deploy workflows run end-to-end, path-filter isolation holds. **T70's automatable portion (unauthed redirect path) verified** via curl in this session; the authenticated walk (sign-in → cross-app navigation → logout) remains operator-led for the next window. **CP14 destructive apply still pending** — `apps/fast/prisma/sql/0002_drop_better_auth_tables.sql` runs against fast's Cloud SQL via Cloud SQL Auth Proxy in a separate operator window gated on T60's backfill populating `portal_sub` for every active user; deploy-first-then-migrate ordering holds (the new revision is already serving and reads nothing from the to-be-dropped tables). **Next code-side pickup: T72 + T73 + T74 + T75** — Phase 6 chrome mount: delete fast's local `components/AppShell.tsx` + `components/layout/Sidebar.tsx` + `components/layout/Header.tsx` in the same commit that rewires `apps/fast/app/layout.tsx` (or the authed wrapper) to mount the React chrome packages CP12 produced — ServiceBar + Sidebar + MobileTopBar + MobileBottomNav + SlideOverNav + AccountWidget — with `data.appCatalog` projected from `loadFastAuthUser`'s response, fast's brand "F" gradient mark in the brand slot, and the visual parity check against heroes + portal at every breakpoint+theme combo. After CP17 closes, T76–T78 (registry + webhooks + taxonomy) open, then Phase 9 (PWA — T84–T86) and Phase 10 (verification + docs — T87–T90) seal Spec 05 at CP21.

> 2026-05-14 (earlier in the same day — **T80 + T81 + T82 SEALED — Spec 05 Phase 8 code-side complete; T83's first `tofu apply` then deferred to operator window** before Op-1 through Op-12 walked it in the same day): Three commits' worth of authoring land as one bundle since T81 references T80's Cloud Run name and T82's declarations live inside T80's module tree — splitting them would have created broken-intermediate states. **T80** authors `infra/fast/` as a 16-file Tofu state mirroring `infra/heroes/`'s shape adapted for fast's single-service Next.js runtime (no `coms-fast-api`; one unified service per ADR 0011 Open Question §3 Option A). `backend.tf` pins state bucket `coms-fast-tfstate` (operator creates pre-first-apply via `gcloud storage buckets create`); `providers.tf` matches heroes at google ~>7.0; `variables.tf` declares the fast-specific surface — project + region defaults match the suite, `fast_db_instance_name` defaults to `aha-fast-db-instance-cd5db712` (legacy pre-merge instance referenced via `data.google_sql_database_instance`, not managed here), seven Secret Manager IDs for DB DSN + Google OAuth client secret + Resend key + Apps Script HMAC + Slack webhook + cron secret + inbound webhook HMAC, plaintext defaults for the public-by-spec Google OAuth client ID + HR sheet identifiers + Apps Script gateway URL + GCS bucket name. `labels.tf` declares `fast_labels` + `fast_labels_web` per standing principle 4. `cloud-run.tf` declares the single `coms-fast-web` Cloud Run service + runtime SA `coms-fast-web-sa` + Cloud SQL client grant + a `for_each` over the seven secret IDs for `roles/secretmanager.secretAccessor` + objectUser on `aha-fast-uploads-prod` + enable iamcredentials API. The container env block uses one `dynamic "env"` for the 13 plaintext values and a second `dynamic "env"` for the seven secret refs (versioned `:latest`). Startup + liveness probes hit `/api/health` (in-container path; Next.js basePath maps the external `/fast/api/health` to internal `/api/health` at serve time); `failure_threshold: 24` on startup gives the bootstrap image headroom pre-T79. `main.tf` wires three modules at `modules/{artifact-registry,monitoring,github-wif}/`: artifact-registry creates `coms-fast-registry` with the same KEEP-5 cleanup policy heroes uses; monitoring takes a single `cloud_run_service_name` string (heroes takes a list because heroes is two services), ships the uptime alert `enabled = false` until T79 lands the health route, and carries a Cloud SQL CPU > 80% alert against the data-source-referenced instance; github-wif declares `coms-fast-wif-pool` + `coms-fast-deployer-sa` + `roles/iam.workloadIdentityUser` for the GitHub principalSet + project-scoped run.admin + artifactregistry.writer + cloudsql.client + secretmanager.secretAccessor + per-runtime-SA serviceAccountUser + serviceAccountTokenCreator (single SA, not the heroes list). `outputs.tf` surfaces Cloud Run URL + AR hostname + SQL connection name + runtime SA + WIF provider + deployer SA emails — the latter two are what `gh variable set WIF_PROVIDER_FAST` and `gh variable set WIF_SA_FAST` consume after the first apply. **`apps/fast/Dockerfile` authored alongside** because fast carried no Dockerfile in the tree (legacy aha-fast had one at root using `node:20 + npm ci --force`; not portable to the Bun workspace and pinned to npm-style lockfile). New Dockerfile mirrors heroes-web's three-stage shape adapted for Next.js standalone — `oven/bun:1` deps stage (workspace-aware COPY of all per-service package.jsons + the Prisma schema before postinstall triggers `prisma generate`), `oven/bun:1` builder stage (`bun run --filter @coms-portal/fast build` runs `next build` with the `output: 'standalone'` flag landed at T65), `node:20-slim` runtime stage (Next.js standalone output is a Node entry; Bun isn't needed at runtime) serving `apps/fast/server.js` on port 3000. **T81** authors `.github/workflows/deploy-fast.yml` at 6.7 KB combining the api + web shapes heroes splits across two files (fast is unified). Path filters on `apps/fast/**` + `packages/{ui-react,account-widget-react,design-tokens,sdk,shared}/**` + workspace lockfiles. Six steps: WIF auth (`WIF_PROVIDER_FAST` + `WIF_SA_FAST`) → setup-gcloud → setup-bun → cache bun install → `bun install --frozen-lockfile` → Prisma `db:push` over Cloud SQL proxy on port 5433 (sed rewrites the host:port segment of fast's DSN from public-IP form to `127.0.0.1:5433`; differs from heroes-api's Unix-socket sed because the stored DSNs differ) → docker build/push to `coms-fast-registry` → `gcloud run deploy coms-fast-web --service-account=coms-fast-web-sa@…`. The destructive-migration caveat comment carries from heroes-api: fast uses `prisma db push` which is non-destructive for additive shapes; the T64-shape `DROP TABLE` needs deploy-first-then-migrate ordering that the migrate-first-then-deploy default doesn't cover. **T82** structurally lives inside T80's `modules/github-wif/`; recorded as a separate task because the operator window will create the deployer SA + WIF pool as distinct GCP resources from the runtime SA. **Verification:** `tofu fmt -check -recursive` clean across `infra/`; `tofu init -backend=false && tofu validate` returns "Success! The configuration is valid." inside `infra/fast/`; YAML parse passes on the new workflow; `bun --filter '@coms-portal/fast' typecheck` stays green; `bun scripts/verify-firebase-json.mjs` stays green (the firebase.json rewrites T69 added pointed at `coms-fast-web` already; now the service has an IaC definition to point at). **What stays for the operator window:** (a) **T83** — create the state bucket (`gcloud storage buckets create gs://coms-fast-tfstate --location=asia-southeast2 --uniform-bucket-level-access`), then `cd infra/fast && tofu init && tofu apply` per `infra/README.md`; (b) operator creates the five new Secret Manager secrets (`aha-fast-google-client-secret`, `aha-fast-resend-api-key`, `aha-fast-apps-script-secret`, `aha-fast-slack-webhook-url`, `aha-fast-cron-secret`) with values currently held as plaintext env on the legacy `aha-fast-app` service — FU-15's password-rotation chain folds into this window since `aha-fast-db-url` carries the prod DB password in cleartext and rotation should run alongside; (c) set the two repo vars (`WIF_PROVIDER_FAST`, `WIF_SA_FAST`) from the apply's outputs; (d) probe push touching `apps/fast/README.md` to confirm path-filter isolation; (e) **T70** runtime verification can finally walk the corridor end-to-end against `aha-coms.web.app/fast/*`; (f) **CP14** destructive SQL applies once T70 returns green. The legacy `aha-fast-app` Cloud Run service stays alive on its separate state until T70 verifies the new revision, then a follow-up window retires it.

> 2026-05-13 (**T65–T69 SEALED — Spec 05 Phase 4 code-side complete; T67 operator-deferred, T70 + CP14 destructive apply deferred to operator window after T81's deploy workflow lands.** Phase 4 walks the single-origin migration mirror of heroes T24–T30 for the React side. **T65** flips `apps/fast/next.config.ts` to `basePath: '/fast'` + `assetPrefix: '/fast'`; the stray `app/fast/page.tsx` child route consolidates into `app/page.tsx` so the post-basePath URL is the clean `/fast/` rather than the doubled `/fast/fast`. **T66** is the absolute-path sweep — 254 client-side `fetch('/api/...')` (and backtick) call sites rewritten to `/fast/api/...` via two sed passes, 8 `router.push('/login')` auth-fallbacks rewritten to `window.location.href = '/portal?app=fast'` (cross-app hard navigation, bypasses Next.js auto-prefixing), 2 `<Link href="/login">` CTAs converted to plain `<a href="/portal?app=fast">`, 3 `<img src="/aha-logo.png">` sites prefixed to `/fast/aha-logo.png` (raw `<img>` doesn't go through basePath), 2 server-side absolute-URL emitters (`app/api/auth/google/callback/route.ts` + `app/api/slack/notify/route.ts`) updated to build `/fast/`-prefixed redirect destinations. **T67** authors `apps/fast/.env.example` from scratch (fast had no env template in the tree) inventorying the 17 env vars `process.env.*` grep surfaces; the actual Google Cloud Console redirect-URI registration (`https://aha-coms.web.app/fast/api/auth/google/callback`) is deferred to the operator window with the rollback-window expectation that the old URI stays registered until the new revision verifies. **T68** authors `apps/fast/middleware.ts` — public-allowlist + `__session` cookie presence check, 401-JSON for `/api/*` callers, `NextResponse.redirect('https://aha-coms.web.app/portal?app=fast&redirect_to=<encoded>')` for page callers, basePath-aware matcher config that skips `_next/static`, `_next/image`, manifest, favicon, and any `.png`/`.svg` asset. **T69** adds the three-rewrite triple to `firebase.json` between heroes' triple and `/api/**` — `/fast/api/**` → `coms-fast-web`, `/fast` → `coms-fast-web`, `/fast/**` → `coms-fast-web` — and updates `scripts/verify-firebase-json.mjs`'s `expected` array to 10 entries. Verifier green. `bun run --filter @coms-portal/fast build` green throughout; the build summary reports `ƒ Proxy (Middleware)` confirming registration. **FU-11 sealed alongside (2026-05-13)** — the 23 pre-existing typecheck errors retired in one pass: `target: ES2017 → ES2022` (clears 9 regex `s`-flag TS1501s and lands fast on the same target as heroes-web/heroes-api/sdk), `UserRow.role` widening to include `'admin'` (4 TS2367s at `app/users/page.tsx`), `viewTicket.custom_fields` typing + optional-chain hardening at `app/nexus/page.tsx` (8 errors), `updatedAt: string` added to two local `Message` interfaces that diverged from `ChannelMessageItem`, `satisfies MoreItem[]` on the `BottomNav` array literal, the duplicate `🔥` key in `EmojiPicker`, and `RefObject<HTMLElement | null>` on `SectionMenu.anchorRef`. The Prisma `InputJsonValue` cluster the original FU-11 entry called out dissolved during T63's auth-lib razing (the file that held it went with it). `bun run --filter '*' typecheck` clean across all 12 packages. **What waits for the operator window:** (a) **T67** operator step — register the new Google OAuth redirect URI in GCC; update Cloud Run's `GOOGLE_REDIRECT_URI` env var via `infra/fast/cloud-run.tf` (Phase 8 / T80) or manual `gcloud run services update`. (b) **T81** in Phase 8 — author `deploy-fast.yml` workflow + the `coms-fast-web` Cloud Run service in `infra/fast/`; without these, the `/fast/*` rewrites in firebase.json point at a service that doesn't exist. (c) **T70** runtime verification — feature-branch deploy + cross-app navigation walk against `aha-coms.web.app/fast/*`. (d) **CP14 destructive apply** — operator runs `apps/fast/prisma/sql/0002_drop_better_auth_tables.sql` against fast's Cloud SQL instance via direct TCP (per T60's runbook); deploy-first-then-migrate ordering means the new fast revision (post-T70) deploys first so no code emits queries against the to-be-dropped tables. PK promotion (`User.id` rename to `portal_sub` cascading 38 FK relations, or `User` → `fast_profiles` per integration contract §2) is the operator's call and gets a separate window. **T60–T64 SEALED — Spec 05 Phase 3 cascade code-side complete; CP14 operator-gated.** Four-commit chain landed the Better Auth cascade across the fast tree: T60 (`60b4e8d`) authored `loadFastAuthUser` + the `portal_sub` schema + the backfill artefacts; T61 (`ec5b594`) flipped 111 server-side `requireAuth` + 1 `getServerSession` site + relocated `useAuth`/`useSession` to a shared substrate behind `/api/auth/me`; T62 (`654da78`) razed the five credential page directories (`/login`, `/register`, `/forgot-password`, `/activate`, `/complete`) — route count down from 22 to 17; T63 (`04ab04f`) cut the four Better Auth lib files (`lib/auth.ts`, `lib/auth-client.ts`, `lib/auth-server.ts`, `lib/auth-context.tsx`) + 4 Better-Auth-route surfaces (`[...all]`, `activate`, `reset-password`, `profile/password`) + removed `better-auth` from package.json — route count down to 14; T64 (`95edacb`) struck the three Better Auth models (`Session`, `Account`, `Verification`) from `schema.prisma` + dropped `User.emailVerified` + cleaned the four writers + authored the destructive SQL (`prisma/sql/0002_drop_better_auth_tables.sql`) for operator apply. **CP14's three predicates resolved on the code-side axis:** (1) every auth path runs through `loadFastAuthUser` ✓, (2) no `Session`/`Account`/`Verification` model declarations remain ✓, (3) `User.id` still carries the Better-Auth-issued string id — the PK promotion to `portal_sub` is deferred to the operator window post-backfill (T64 reads `[~]` in todo.md to record the partial seal). **Operator window dependencies:** (a) T60's backfill script runs against prod (`apps/fast/scripts/backfill-portal-sub.ts` with the portal-exported CSV) to populate `portal_sub` for the existing user population; (b) `prisma/sql/0001_add_portal_sub.sql` adds the column to prod (idempotent); (c) the new fast revision deploys first (post-CP14 code that touches no dropped tables); (d) `prisma/sql/0002_drop_better_auth_tables.sql` runs second via Cloud SQL Auth Proxy — drops the 3 tables + `emailVerified` column; (e) the operator decides whether to rename `User` → `fast_profiles` and promote `portal_sub` to PK (the 38-FK cascade is non-trivial and deserves its own commit). **Runtime caveat that holds until Phase 4 lands:** fast's new auth path requires single-origin reachability of portal-api's `/api/userinfo`. Phase 4 (T65–T70) wires that. Until T70, redeploying fast standalone breaks auth — documented in T61's outcome + the commit body so the next hand-off doesn't surprise. T65 (basePath: '/fast' in next.config.ts) is the next code-side task once CP14 closes. **CHECKPOINT 11 CROSSED — Spec 02 SEALED** (earlier in 2026-05-13). `apps/fast/lib/auth/load-fast-auth-user.ts` mirrors heroes-shared's `loadHeroesAuthUser` line-by-line: fetch `${portalOrigin}/api/userinfo` with the forwarded `__session` cookie, 401-returns-null contract, `PortalSessionDeniedError` thrown when the `apps` claim lacks the `fast` slug, Prisma upsert keyed on `portal_sub`. Returns `{ user, appCatalog }` where `user` carries the nine fields T61's 137 `requireAuth`/`getServerSession`/`useAuth` call sites read (`id`, `portalSub`, `email`, `name`, `image`, `role`, `teamId`, `portalRole`, `apps`). Cookie reading stays in the caller (Next.js Server Components/Route Handlers) so the helper is server-runtime-agnostic + unit-testable — matches heroes' `hooks.server.ts`-reads-then-passes pattern. Prisma schema gains `portal_sub String? @unique` on `User` (nullable so existing rows survive; field name flat, not `@map`-translated, so the surface reads identically across helper + backfill + T61 flip). Auditable SQL recorded at `apps/fast/prisma/sql/0001_add_portal_sub.sql` (idempotent `ADD COLUMN IF NOT EXISTS` + partial unique index) — fast uses `prisma db push` per package.json, so this is the source-of-truth artefact rather than a Prisma migration. Backfill script `apps/fast/scripts/backfill-portal-sub.ts` joins fast's `User.email` (lowercased+trimmed) against a portal-exported CSV of `(email_normalized, identity_user_id)` pairs, with `--dry-run` flag for safety + per-row unique-index conflict handling. Cross-database friction (portal-Drizzle vs fast-Prisma; both on the shared Cloud SQL instance but Postgres cross-DB SQL is FDW territory) resolved by the two-step operator dance: psql `\copy` exports from portal first, then `bun run` consumes the CSV against fast. TDD red→green via `apps/fast/lib/auth/load-fast-auth-user.test.ts` — five bun:test cases (valid session+fast slug, 401-null, denied-apps throws, server-error throws, cookie-forwarding). `bun test` green; `bun run --filter @coms-portal/fast build` green (22 routes prerender); workspace-wide typecheck shows zero new errors (only the pre-existing FU-11 set). Operator runbook (column add via Cloud SQL proxy → CSV export from portal → bun-run backfill --dry-run then real run → record the still-null operator-outreach list) deferred to the operator window. **CHECKPOINT 11 CROSSED — Spec 02 SEALED.** FU-8's three rows resolved across the operator window: row 1 (sheet-sync admin write) deferred to FU-9 with the orthogonality-to-Phase-5 record after the operator click surfaced the IaC-drift + silent-fail-handler + wrong-i18n-key triple (the i18n key fix landed at commit `791d073`; the IaC + silent-fail layers tracked separately as FU-9); row 2 (heroes p50/p95) closed green with cold p50 ~788ms server-wait / p95 ~1290ms / DCL p50 ~1370ms / p95 ~1800ms across 5 cold + 5 warm samples (warm indistinguishable from cold per the architectural shape — bottleneck is per-request server-side work, not cacheable browser-side); row 3 (on-device PWA install) closed with structural install paths verified in isolation (Chrome mobile installs both portal and heroes as proper PWAs with correct icons + splash + standalone chrome), surfacing the shared-origin two-PWA coexistence conflict — portal-web's manifest declares neither `id` nor `scope`, so its default scope `/` transitively includes `/heroes/`, and Chrome refuses heroes' install with "this app is already installed". Confirmed by uninstall-portal-then-try-heroes producing a clean heroes install. Lifted to FU-10 as the structural fix (Option A recommended: portal-web mounts at `/portal/` analogous to heroes at `/heroes/`, scope-overlap-free for unlimited future apps at `/<app>/`). **FU-10 is load-bearing for Spec 05's framework-parity claim** — fast at `/fast/` will hit the same conflict; cross-reference noted in FU-10's body. CP11's contract-gate sections (§§ 1–9 + 11–14) all close on what was done in-session and across the operator window. Two follow-ups remain open (FU-9 sheet-sync silent-fail; FU-10 shared-origin coexistence) — operationally important but contract-orthogonal, both documented with structural-fix shapes for the sessions that pick them up. **Spec 02 complete.** **Phase 6 originally sealed in three commits — T47 + T48 + T49 all `[x]`; CP11 gated on FU-8 alone, the consolidated operator-driven runtime verification.** This session closed the two open follow-ups: **FU-7 (commit `3bb61f2`)** — the portal-api "196 failures" diagnosed as a `bun test` vs `bun run test` runner-invocation footgun, not a test-suite bug. Bun's `mock.module()` is process-global; when the bare invocation collapses all 63 files into one process, `route-compose.test.ts`'s import of the real `src/index.ts` poisons the module cache and every later file's `mock.module` calls silently no-op. CI runs the canonical `bun run test` (xargs -P 4 per-file) and reports 575/0; the 196 number is a runner artefact. Root `bunfig.toml` gains a strong comment naming the canonical invocation + the contamination mechanism; new `apps/portal-api/README.md` greets contributors with the same signage. Structural alternative (refactor `route-compose.test.ts` to mock auth/db instead of importing the real composed app) intentionally deferred. **FU-6 (commit `5df692e`)** — both apps now meet Chrome's PWA installability spec. Portal-web's manifest had referenced `/icon-{192,512}.png` files that never existed (`apps/portal-web/static/` carried only the manifest); generated via ImageMagick as gradient `#1D388B → #325FEC` rounded squares with white "C" in Helvetica-Bold matching the brand mark in `(authed)/+layout.svelte:140`. Heroes-web's icons were 1×1 px stubs in `static/icons/`; replaced with gold-gradient `#FFD97D → #F4C144` rounded squares + deep-gold "H". Heroes manifest hardened — `start_url`, `scope`, `id` all `/heroes/` so Chrome registers heroes-web as a distinct install registration from portal-web; icon `src` paths absolutised because SvelteKit's `paths.base` doesn't auto-prefix manifest values. Fresh `apps/heroes-web/src/service-worker.ts` mirrors portal-web's cache-on-install pattern, API-skip-guard reshaped for `/heroes/api/*`. Heroes-web meta theme-color realigned `#1D388B → #09090b` to match the manifest. Stale `apps/heroes-web/public/` directory razed entirely (4 files — Next.js-era manifest with wrong branding, orphaned hand-rolled sw.js, solid-blue placeholder icons; grep confirmed nothing referenced them). Both apps' typecheck stays green. **T49 (commit `d8bfda1`)** — the heroes corridor carved into prose. `apps/heroes-web/README.md` opens as the reference implementation of §§ 1–9 + 11–14, walks `loadHeroesAuthUser` end-to-end (`__session` → `/api/userinfo` → `heroes_profiles` upsert), names every door deliberately closed (no `session/account/verification` tables, no `/auth/portal/exchange`, no login routes, no `password/email_verified/provider_id` columns, no `coms_session` writes, no hardcoded app catalog), includes a §"ADR 0005 vs reality" note recording T31's finding that `__session` is opaque not JWT (the stateless-on-the-app-side guarantees stand verbatim, only the introspection primitive differs), and an integration-contract cross-reference table pinning each § to a concrete file:line anchor. `apps/heroes-api/README.md` is the short companion (mirrors portal-api's shape; points back at heroes-web's README for auth). The original `coms_aha_heroes` carried `CONTEXT.md/DESIGN_SYSTEM.md/TODOS.md` but no `README.md`; T49 authored from scratch against current state, no stale prose to remove. **T47 sealed `[x]`** — chrome-parity + auth + admin-load paths covered the contract gate; admin-write row consolidated into FU-8. **T48 sealed `[x]`** — perf-check scope folded into FU-8 (operator-driven runtime measurement; expectation per Phase 5's auth-path JOIN cut is flat-or-faster). **FU-8 opened** — single consolidated entry carrying the three operator-only runtime checks (sheet-sync admin write, heroes p50/p95 measurement, on-device PWA install verification on Brave + Chrome mobile for both portal-web at `/` and heroes-web at `/heroes/`). CP11 crosses when an operator runs FU-8's three checks against prod and reports each row green; until then, the contract-gate sections of CP11 (§§ 1–9 + 11–14) are met by in-session verifications, but the cross-the-checkpoint moment waits on the operator window. **Pre-session state recorded for cross-reference:** **T47 was originally in flight** — 4 of 5 smoke checklist items verified; the end-to-end smoke walked through sign-in → cross-app navigation → logout (all PASS) and surfaced five Findings on the way; four mended same-session, one deferred. **Finding 1 (commits `e6075ce` + `b57a6cb`):** all six admin pages (`/heroes/admin/{users,users/[id],settings,audit-log,sheet-sync,reports}`) were 500-ing in prod since Phase 1's `da988a9` — `SyntaxError: Failed to parse JSON` thrown from `+page.server.ts` loads that used `event.fetch('${base}/api/v1/...')`. Phase 1's single-origin migration made SvelteKit treat that as same-origin against heroes-web's own router (Firebase forwards the run.app Host header), which has no `/api/v1/*` route, returns 404 HTML, and `res.json()` chokes. Six admin loads converted to direct service imports (the pattern `dashboard` + `teams` always used); five `.svelte` files' aspirational casts gained `as unknown as` wrappers because the local types referenced fields the API never actually returned (`teamName`, `teamId`, `finishedAt`, `error`, `department`, `createdAt`) and always rendered as `—`. **Finding 2 (commit `4fb2069`):** mobile chrome stacked two top bars carrying duplicate theme+avatar widgets — `ServiceBar.svelte:45` shipped without a responsive hiding class while `MobileTopBar.svelte:39` had `md:hidden`. One-class fix: `flex` → `hidden md:flex` on ServiceBar; `MobileTopBar` moved from `top-9` to `top-0`; both apps' outer `pt-9` → `pt-0 md:pt-9`. Cross-app tab strip is now desktop-only on intent; mobile users switch apps via the AccountWidget dropdown. **Finding 3 (commit `bcf0c27`):** portal-web's MobileBottomNav surfaced only 3 of 7 admin sections; the other 4 (Teams, Aliases, Taxonomies, Audit Log) had no mobile path. Heroes had a hamburger + slide-over for the equivalent gap (T43); portal-web inherited T43's "no admin mobile surface" framing alongside it. The slide-over graduated into `packages/ui-svelte/src/chrome/SlideOverNav.svelte` (second concrete consumer retired T43's "one consumer, don't lift" call) — takes `items`, `currentPath`, `brand` + `footer` snippets; `Sheet` primitive carries backdrop / focus trap / ESC / slide-in. Heroes' inline 60-line `<Sheet>` block collapses to a `<SlideOverNav>` call; portal-web's `MobileTopBar` gains a leading hamburger (admin-only) plus a sibling `<SlideOverNav>` carrying `BASE_NAV + ADMIN_NAV`. **Finding 5 (commits `9fd569e` then `58222ba`):** Finding 2 hid ServiceBar on mobile, which removed heroes users' path back to portal — `data.appCatalog` from `/api/userinfo` carries registered apps only (portal isn't in `app_registry`). First-pass fix prepended `{slug:'portal',label:'COMS'}` to heroes' `widgetAppSwitcher` in the layout; operator immediately surfaced that every future app would have to repeat the same prepend in two places. Final fix lifted the hub knowledge into `apps/portal-api/src/routes/userinfo.ts` — the launcher response always prepends `{slug:'portal',label:'COMS',url:'/'}` unconditionally. Every consuming app sees portal as the first entry of `data.appCatalog`; no app-side prepend code anywhere. Heroes' (authed)/+layout.svelte loses 22 lines across two derived blocks. The standing principle for future apps onboarding: read `data.appCatalog` and iterate; no special-casing required for portal. The accumulated lesson across T40 (ServiceBar derivation lift), T43 (slide-over deferral then lift), and Finding 5 (hub-in-userinfo) is consistent — hub-aware knowledge belongs in canonical sources, not duplicated in every app's layout. **Finding 4 deferred** to FU-6: PWA install prompt not firing on the operator's mobile browser. The chrome-parity smoke completed without needing it; installability is a packaging concern outside CP11's `§§ 1–9 + 11–14` scope. **FU-7 surfaced from the smoke verification path:** `apps/portal-api` test suite reports 379 pass / 196 fail / 575 total across 63 files. `git stash` confirmed the 196 fails predate today's work; spread across webhook reactivate, manifest, smoketest, aliases-resolve-batch, taxonomies-sync routes — fixture drift or genuine regression not caught by the test-runner scope CI uses. Out of T47 scope; tracked separately so the next session can audit `.github/workflows/ci.yml` and triage. **What remains for T47:** (1) operator runs one admin write on heroes (sheet-sync trigger is the canonical exercise, confirms `services/sheet-sync` end-to-end including the post-Phase-5 `heroes_profiles.can_submit_points` read path), (2) call on FU-6 — seal T47 with PWA deferred, or hold T47 open until FU-6 closes (recommend: seal — PWA is out of CP11 scope). **Then T48** (perf check — operator measures heroes p50/p95 page load; expectation is flat-or-faster after Phase 5 cut the per-request JOIN from 2 tables to 1) **and T49** (heroes README rewrite — point at `docs/integration-contract.md`, document the cleaned auth flow as the reference pattern, drop the stale local-session prose). After T49, **CP11 crosses and Spec 02 is sealed.** **Checkpoint 10 crossed — Spec 02 Phase 5 sealed.** T44 + T45 + T46 cleared the cache corridor in three commits: T44 wrote the decision matrix into `tasks/todo.md` (every column on `email_cache` + `user_config_cache`, every key actually read out of the JSONB blob), T45 promoted `canSubmitPoints` onto `heroes_profiles.can_submit_points` (migration `0017_add_can_submit_points.sql` mirrors `0013`'s shape — column add + `COALESCE` backfill out of `user_config_cache.config->>'canSubmitPoints'`), and T46 dropped the table outright (migration `0018_drop_user_config_cache.sql`, plus the schema file, the typebox helpers in `schemas/index.ts`, the `generate-schemas.ts` row, and the `re-exports from db/schema/index.ts`). Net outcome: `loadHeroesAuthUser`'s SELECT collapsed from `heroes_profiles ⋈ user_config_cache` (2 tables, 1 leftJoin) to a single read against `heroes_profiles`; the three repositories/users.ts read sites (`listUsers`, `getUserById`, `getUserByEmail`) dropped their `leftJoin(userConfigCache)` and now read `heroes_profiles.canSubmitPoints` directly; the two stale HR-lookup queries in `services/challenges.ts:90` + `services/appeals.ts:77` flipped from `userConfigCache.config->>'role' = 'hr'` to `eq(heroesProfiles.role, 'hr')` (the role column has carried this since `0013`, the cache JOIN was vestigial); the two webhook handlers (`handle-user-provisioned`, `handle-app-config-updated`) write `can_submit_points` to `heroes_profiles` and no longer touch the dropped table. **Per-request auth-path table touches: was 2, now 1.** UserRow.canSubmitPoints tightened from `boolean | null` to `boolean` (NOT NULL column). The audit's three carry-overs all resolved in the same window: `role` duplication retired (read sites flipped), `leaderboard_eligible` + `starting_points` retired with the table (zero readers, no replacement needed), the manifest doc-debt deliberately deferred for a future portal-facing manifest-cleanup window. **Spec 02 §22's "JWT claims" framing** didn't survive contact with T31's CP6 finding (`__session` is opaque, not a JWT); the equivalent contract is the `/api/userinfo` response shape `loadHeroesAuthUser` already reads, and `canSubmitPoints` is a heroes-specific knob that belongs on the heroes-owned table — recorded inline in `tasks/todo.md`'s T44 note for future plan readers. Deploy applied 2026-05-12 in the same window CP10 sealed: heroes-api new revision rolled out via GHA run `25742283576` (the laptop push of `1b79454` triggered it on path-filter; the old workflow had no migrate step yet so the deploy ran first by accident-of-history but matched the required order for T46's destructive drop), then `cloud-sql-proxy` + `bun --filter @coms-portal/heroes-api db:migrate` against prod applied `0017` + `0018` in sequence — backfill found zero rows with `canSubmitPoints=true` (clean baseline; webhooks will flip the flag as `app_config.updated` events arrive), then dropped the cache table. Post-apply schema verified by direct read: `to_regclass('user_config_cache') = null`, `heroes_profiles.can_submit_points` lives as `boolean NOT NULL DEFAULT false`, 72 rows in heroes_profiles. **FU-5 closed in the same commit window** — `deploy-heroes-api.yml` gains the migrate step mirroring portal-api's FU-3 shape (different secret name `coms-aha-heroes-db-url-production`, different DSN host rewrite for heroes' `@localhost/db` shape, same Cloud SQL instance shared by both APIs); the manual ritual that ran for Phase 5 retires alongside the work it served. Next: T47 — Phase 6 verification opens with the E2E smoke checklist (sign-in → heroes; navigation; cross-app logout; mobile PWA chrome; admin operations). **Checkpoint 9 crossed — Spec 02 Phase 4 sealed.** T40 → T43 cleared the application-layer glue heroes carried since the polyrepo: T40 lifted the ServiceBar derivation into `packages/ui-svelte/src/chrome/derive-services.ts` (pure catalog→items mapper; rich `apps` array crosses through `HeroesAuthResult` to `App.Locals.appCatalog`, so the hardcoded `[{slug:'portal',href:'/'},{slug:'heroes'}]` literal and the `APP_LAUNCHER × user.apps` map both retire). T41 lifted the theme narrowing into `packages/ui-svelte/src/chrome/resolve-theme.ts` (`'system'|'light'|'dark'` → `'light'|'dark'`; ServiceBar + MobileTopBar widen their `theme` prop and call the helper internally; heroes' `effectiveTheme` $derived shim disappears). T42 migrated the workspace from `lucide-svelte` (Svelte 4 class components) to `@lucide/svelte ^1.14.0` (Svelte 5 native) — 49 files swept via sed, 3 package.json deps swapped, all 26 `as AnyIcon` casts + the `type AnyIcon = any` alias + heroes' "version-skew artefact" comment retire, portal-web's `type IconComponent = unknown` workaround + two `as never` casts collapse in the same window. T43 kept the slide-over admin menu heroes-local (rationale: only one concrete Svelte consumer today; aha-fast is React; portal-web has no admin mobile surface; premature abstraction otherwise) but adopted the suite's already-paid-for `Sheet` primitive at `packages/ui-svelte/src/primitives/sheet/` (bits-ui-backed) so heroes stopped hand-rolling backdrop + focus-trap + ESC + slide-in animation; ~60 lines of hand-rolled drawer DOM collapse to ~35 lines around `<Sheet><SheetContent side="left">…</SheetContent></Sheet>`, the global `<svelte:window onkeydown>` ESC handler retires with the hand-rolled panel. Portal-web is touched only by T42's package swap + T43's carry-along cast cleanups; the broker-formAction derivation is deliberately untouched (waits for a future window when every registered app is provably same-origin and the broker hop retires). (**Checkpoint 6 crossed — Spec 02 Phase 1 sealed, Phase 2 partial.** Sign-in → /heroes/dashboard renders end-to-end on the new single-origin + portal-`__session` auth path.) T30 verification dragged Phase 2 forward by force: Firebase Hosting filters every incoming cookie except `__session` before forwarding to Cloud Run (hardcoded behaviour), so heroes' `coms_session` cookie set on the exchange response landed in the browser cleanly but never travelled back on the redirect to `/heroes/dashboard` — the portal↔heroes loop. Spec 02's Phase 2 was the spec'd answer to that wall; T31's audit revealed portal's `__session` is an opaque UUID (`auth_sessions.id`), not a JWT, so introspection goes through `apps/portal-api/src/routes/userinfo.ts` (`GET /api/userinfo`) rather than SDK-side JWT verification. T32 landed `loadHeroesAuthUser` in `packages/heroes-shared/src/auth/user.ts` — single fetch + heroes_profiles upsert + heroes-side fields read. T33/T34 retired the `getLocalSessionByToken` JOIN paths in heroes-web's hooks and heroes-api's auth middleware; both now call `loadHeroesAuthUser` with `env.PORTAL_ORIGIN`. `+page.server.ts` short-circuits the legacy exchange when `__session` already authenticated the request. T37 sealed in incognito 2026-05-12. **Five infra mends shipped during the verification cycle that weren't on the plan:** the bare `/heroes` Firebase rewrite (slash-less launcher target), `PORTAL_ORIGIN` Cloud Run env (was `PORTAL_BASE_URL` mismatch), portal-web SW skip-list for `/heroes/*` (was intercepting and dropping Set-Cookie), Cache-Control `private, no-store` on heroes-web's three cookie-write handlers (so Firebase stops stripping Set-Cookie on legacy paths), and the probe-path migration in `infra/heroes/cloud-run.tf` + the uptime check (live Cloud Run probes hit the new `/heroes/api/healthz` + `/heroes/api/health` paths). Operator window applied them in three deploy cycles; `register:heroes` re-run upserted `app_registry.url`, `healthCheckUrl`, and `app_webhook_endpoints.url` to the single-origin URLs. **T35/T36 owed** to close CP7 cleanly: the local `session`/`account`/`verification` tables hold no live data after this seal, `getLocalSessionByToken` family is dead code, and the legacy `coms_session` Set-Cookie cluster will rust. Next: drop the dead code (T35) + drop the dead tables (T36). **CP7 + CP8 crossed in the same window** — T35 deleted `packages/heroes-shared/src/auth/session.ts` outright, swept the six call sites (sheet-sync admin path uses `loadHeroesAuthUser`, both portal-event handlers stop touching local sessions, exchange + logout routes deleted, logged-out page simplified), and pulled the `./auth/session` subpath from `package.json`. T38/T39 folded forward — the `/auth/portal/exchange` route is gone, the route directory removed; portal's `portal_code` query arrives ignored. T36 lands `0016_drop_legacy_auth_tables.sql` (the three DROP TABLE IF EXISTS statements + rollback note) and deletes `packages/heroes-shared/src/db/schema/auth.ts` + the re-exports + the three typebox schemas. Drizzle's `db:generate` was abandoned in favour of a hand-written migration to keep the diff legible. Apply order at deploy: heroes-api new revision deploys first (no code touches the tables anymore), operator runs `bun db:migrate` against prod via Cloud SQL Auth Proxy second. T24 set `kit.paths.base: '/heroes'` on heroes-web. T25 swept ~25 literal `/`-rooted paths across routes + lib components — every `<a href>`, `goto()`, `redirect()`, `fetch()` now flows through `$app/paths` `base` so SvelteKit doesn't black-hole them against the new mount point. T26 wrapped heroes-api in `new Elysia({ prefix: '/heroes' })`; route URLs land at `/heroes/api/*` to match Firebase Hosting's preserve-path rewrite. T27's potential fan-out collapsed into one line in `apps/heroes-web/src/lib/api/client.ts`: the typed `App.~Routes` now wears a leading `heroes` segment (Elysia's `CreateEden<BasePath, …>` rule), so the eden client pre-traverses into it (`treaty<App>('').heroes`) and every existing `api.api.v1.*` call keeps its shape. T28 was a no-op — the rewrite stack from CP4 already routes `/heroes/api/**` → `coms-heroes-api` ahead of the broader `/heroes/**` → `coms-heroes-web` line. T29 stripped `data.portalOrigin` + `data.heroesOrigin` out of `(authed)/+layout.svelte` and `+layout.server.ts`; service-bar links collapsed to path-relative (`{ slug: 'portal', href: '/' }`), nav arrays were rewritten under `${base}`, `AccountWidget` now receives `portalOrigin=""` (same-origin) and `postLogoutRedirectUri={\`${$page.url.origin}${base}/logged-out\`}` — the absolute form is required by portal's `validatePostLogoutRedirectUri`, the spec's "path-relative" wording fails the `new URL(uri)` parse and was logged as drift for the next doc-rev sweep. Infra carry-over: probe paths (`infra/heroes/cloud-run.tf` startup + liveness, `infra/heroes/modules/monitoring/*` uptime check) repointed to `/heroes/api/healthz` + `/heroes/api/health` — they hit the Cloud Run host directly without Firebase rewriting, so they MUST land before the next heroes-api deploy or the new revision fails its startup probe. **CP6 still red.** Next: T30 cutover window — `tofu apply` in `infra/heroes/` first, then GHA deploys for heroes-api + heroes-web, then `register:heroes` re-run with `HEROES_APP_URL=https://aha-coms.web.app/heroes` + `HEROES_WEBHOOK_URL=https://aha-coms.web.app/heroes/api/webhooks/portal` + `HEROES_HEALTH_CHECK_URL=https://aha-coms.web.app/heroes/api/health`, then verify the corridor end-to-end (sign-in → `/heroes/dashboard` → cross-app link → logout). After T30 closes, CP6 crosses and Phase 2 (JWT sessions) opens.
> Status: Phase 1 sealed at `34fbedd`; Phase 3 sealed and Checkpoint 2 crossed at `33593a9`; T16 lands the portal split + per-service Cloud Build scaffolding at `5935d00`; T16.5 splits `infra/heroes/` at `de68b28`; the per-app-resources + per-app naming principles + GHA-over-Cloud-Build + standard label set principles are the standing rules going forward; T17 returned deploys to GitHub Actions; Checkpoint 3 crossed 2026-05-12 — heroes + portal Tofu applied (T16.5 + T17 cutovers landed in one operator window), four GHA workflows verified end-to-end, path-filter isolation proven by two single-file probe pushes; Phase 4 sealed at `e129cfb`; T18 authors the Firebase Hosting routing layer (`firebase.json` + `.firebaserc` + `firebase-public/` stub + a `scripts/verify-firebase-json.mjs` guard); hosting site renamed from the spec's `aha-coms-staging` to `aha-coms` (no separate staging tier exists today); T19 deployed `https://aha-coms.web.app` 2026-05-12 with all four corridors verified by the routing probe; portal-web Firebase init build-arg bug surfaced under first user-facing touch and was mended at `41aeb6e` (workflow now fetches `coms-portal-gip-api-key` from Secret Manager and passes three `--build-arg`s); T20 confirmed `__session` cookie crosses from portal-web sign-in to `/heroes/*` requests (HostOnly, Path `/`, HttpOnly, Secure, SameSite Lax). **Checkpoint 4 crossed — Phase 5 sealed.** Three Findings from T20 carried to Spec 02 Phase 1 (stale heroes app_registry URL, portal-web SW intercepts `/heroes/*`, opaque session-id token vs JWT). Findings 1 + 2 from T15 closed in T16. FU-1 + FU-2 settled 2026-05-12: FU-1 adds `app_registry.healthCheckUrl` (migration `0035_naive_solo.sql`) + threads it through the probe + `register-heroes` upserts it; FU-2 rewrites `apps/portal-web/cloudbuild.yaml` to mirror the GHA shape (single `coms-portal-gip-api-key` fetch + plain-value substitutions for the two public Firebase web-config fields), adds a Cloud Build SA secret-accessor grant in `infra/iam-portal-runtime.tf` (re-aimed at the Compute Engine default SA after the legacy `cloudbuild.gserviceaccount.com` proved unused on post-April-2024 projects), inlines the nested `_IMAGE` substitution (Cloud Build does not recursively expand `${...}` inside other substitutions), and drops the machine type to UNSPECIFIED so the rare-path hatch lives in the free-tier 120-min/day budget. Prod-applied 2026-05-12 in one window: `tofu apply` (IAM grant), `db:migrate` against the live DB via Cloud SQL proxy, `register:heroes` re-run (drift detected on `healthCheckUrl: (null) → coms-heroes-api…/api/health`), test build `f313eefb-…` STATUS: SUCCESS in 4m38s on the free-tier machine deploying `coms-portal-web-00008-xzk`. Commit `36f2a41` captured the two latent cracks that emerged under live submission. After this window, the new portal-api revision deployed by GHA finally consults `healthCheckUrl` and flips the HEROES dashboard card green on the next 60-second probe. Next: Phase 6 (archive external repos — T21–T23).
> Source specs: `docs/spec/01-monorepo-consolidation.md`, `docs/spec/02-heroes-cleanup.md`

## Goal

Move the COMS suite from the current polyrepo state (5 external lib repos + 2 external app repos + portal as the consolidation host) to a single Bun workspace monorepo at `aha-coms/`, with heroes cleaned up to be the reference implementation for the integration contract.

Total scope: 12 phases (Spec 01: 6 phases, Spec 02: 6 phases) broken into 49 vertically-sliced tasks in `tasks/todo.md`, with 11 hard checkpoints between phases.

## Inputs (read first in any fresh session)

In order:

1. `docs/integration-contract.md` — the rules every service must satisfy. The destination state.
2. `docs/spec/01-monorepo-consolidation.md` — the structural move.
3. `docs/spec/02-heroes-cleanup.md` — the heroes alignment.
4. `docs/adr/0001..0010` — the why behind load-bearing decisions; reference as needed for each task.
5. This file (`tasks/plan.md`) and `tasks/todo.md`.

## Current state (as of CP1 + T10 commit `9426307`)

**On disk** (in `~/HT/AHA COMS/`):

- `aha-coms/` — Bun workspace with `apps/{api,web}` and `packages/{shared,design-tokens,sdk,ui-svelte,account-widget-svelte,ui-react,account-widget-react}`. The consolidation host with seven packages now resolved through `workspace:*`.
- `aha-fast/` — Next.js project management tool, npm-based, separate repo. **Not in Spec 01 scope.** Onboarding is a future spec (post-Spec 02).
- `coms_aha_heroes/` — SvelteKit + Elysia, Bun workspace, separate repo. Still external; T11 begins the subtree-merge.
- `coms-{sdk,shared,ui,design-tokens,account-widget}/` — five external lib repos, **subtree-merged in-tree** but **not yet archived on GitHub** (Phase 6 / T21).

**Already complete:**

- Phase 2: renamed `coms_portal` → `aha-coms` (doc set v1, commit `981ae02`).
- Phase 1: all five lib subtree-merges + two React stubs + apps/{api,web} workspace flip + SDK gap closure + SDK example-test fix. Crossed Checkpoint 1 at `48649c5`, healed in `34fbedd`.
- T10: heroes freeze window — sole maintainer; freeze trivially in effect (`9426307`).

**Not started:**

- Spec 01 Phases 3, 4, 5, 6 — T11 is next (subtree-merge `coms_aha_heroes`).
- All of Spec 02.

**Known pre-T11 caveat:** three non-main remote branches exist on `coms_aha_heroes` (`ci/parallelize-and-harden`, `ci/skip-redundant-build-and-docker-parallel`, `rev3/spec-01-02-adoption`). User confirmed 2026-05-11 they hold no work that needs preserving; `git subtree add ../coms_aha_heroes main` will not pull them and they will be archived alongside the repo at T22.

## Dependency graph (Spec 01 → Spec 02)

```
Spec 01 Phase 1: libs into aha-coms
   subtree shared (no deps)              ┐
   subtree design-tokens (no deps)       │ — can be parallel
   subtree ui-svelte (consumes tokens)   │
   subtree account-widget-svelte         │
   subtree sdk (depends on shared)       │ — must follow shared
   stub ui-react (empty)                 │
   stub account-widget-react (empty)     │
   convert apps/{api,web} git URLs → workspace:*
   resolve apps/api SDK gap (0.1.1 → current)
        ↓
   CHECKPOINT 1: portal-api + portal-web build green
        ↓
Spec 01 Phase 3: heroes into aha-coms (after freeze with heroes' eng)
   subtree coms_aha_heroes → temp location
   restructure → apps/heroes-api + apps/heroes-web + packages/heroes-shared
   rename @coms/* → @coms-portal/heroes-*
   convert heroes' git URLs → workspace:*
   verify SSO end-to-end
        ↓
   CHECKPOINT 2: heroes builds + SSO works
        ↓
Spec 01 Phase 4: per-service path-filtered Cloud Build
   each apps/<service>/cloudbuild.yaml → installs from monorepo root
   Cloud Build triggers updated with includedFiles filters
   verify single-app PRs trigger only that app's build
        ↓
   CHECKPOINT 3: per-service deploys verified independent
        ↓
Spec 01 Phase 5: Firebase Hosting staging
   firebase.json with /heroes/**, /api/**, /** rewrites
   deploy to staging
   verify cross-app cookie sharing
        ↓
   CHECKPOINT 4: single-origin routing works in staging
        ↓
Spec 01 Phase 6: archive external repos
   GitHub-archive 5 lib repos + coms_aha_heroes
   update repository.url in in-tree package.jsons
        ↓
   CHECKPOINT 5: Spec 01 complete
        ↓
Spec 02 Phase 1: heroes single-origin migration
   kit.paths.base: '/heroes' in heroes-web
   audit internal links for base-path compliance
   heroes-api Elysia router → /heroes/api prefix
   heroes-web eden client config updated
   Firebase Hosting rewrites updated for /heroes/api/**
   layout: ServiceBar derives from APP_LAUNCHER; drop portalOrigin
        ↓
   CHECKPOINT 6: heroes lives at /heroes/* same-origin
        ↓
Spec 02 Phase 2: heroes JWT sessions
   confirm SDK JWT payload contract; update SDK if needed (do this FIRST)
   write loadHeroesAuthUser() in packages/heroes-shared
   replace hooks.server.ts + middleware/auth.ts with JWT path
   remove getLocalSessionByToken family from session.ts
   migrate: drop session/account/verification tables
   verify auth E2E
        ↓
   CHECKPOINT 7: heroes has no local session tables
        ↓
Spec 02 Phase 3: portal handoff for first-login
   audit /auth/portal/exchange role
   refactor or delete (no DB write, no session mint)
        ↓
   CHECKPOINT 8: no app-local session minting
        ↓
Spec 02 Phase 4: chrome lib glue absorption
   serviceBarServices derivation → chrome lib
   theme narrowing → chrome lib
   icon type cast removed (version-skew gone)
   decide: slide-over admin menu local vs generalize
        ↓
   CHECKPOINT 9: heroes layout file shrinks meaningfully
        ↓
Spec 02 Phase 5: cache evaluation
   map email_cache + userConfigCache fields → JWT / keep cached / migrate
   migrate JWT-eligible data into JWT claims
   reduce per-request 3-table JOIN to 1
   keep webhook events table
        ↓
   CHECKPOINT 10: auth-path query reduced
        ↓
Spec 02 Phase 6: verification + docs
   E2E smoke test
   performance check (p50/p95 flat or faster)
   update heroes' README to point at integration contract
        ↓
   CHECKPOINT 11: heroes is the reference implementation
        ↓
Spec 05 Phase 1: React chrome packages
   audit Svelte chrome surface (T50)
   port 5 chrome + 1 widget + Sheet primitive to React (T51, T52)
   author stub consumer (T53)
   visual parity check at desktop/mobile/light/dark (T54)
        ↓
   CHECKPOINT 12: React chrome at visual parity with Svelte
        ↓
Spec 05 Phase 2: subtree merge fast into the monorepo
   confirm freeze (T55)
   subtree-merge → restructure → rename package (T56, T57)
   npm → Bun workspace conversion (T58)
   verify install/build/dev/test (T59)
        ↓
   CHECKPOINT 13: fast lives in-tree at apps/fast/
        ↓
Spec 05 Phase 3: Better Auth removal (3-staged per ADR 0011's decision)
   (a) author loadFastAuthUser; add portal_sub column, backfill (T60)
   (b) flip 127 requireAuth call sites to loadFastAuthUser (T61)
   (b) delete 5 credential routes (T62)
   (c) delete 4 auth lib files + proxy.ts (T63)
   (c) drop Session/Account/Verification models; promote portal_sub PK (T64)
        ↓
   CHECKPOINT 14: fast has no Better Auth surfaces
        ↓
Spec 05 Phase 4: single-origin migration (mount fast at /fast/)
   basePath: '/fast' in next.config.ts (T65)
   audit + sweep internal links + fetches through basePath (T66)
   register Google Calendar OAuth redirect URI for /fast/ (T67)
   author middleware.ts (replacement for proxy.ts) (T68)
   firebase.json: /fast/api/** + /fast/** rewrites in order (T69)
   verify against feature-branch deploy (T70)
        ↓
   CHECKPOINT 15: fast lives at aha-coms.web.app/fast/*
        ↓
Spec 05 Phase 5: Prisma → Drizzle migration (skipped per ADR 0011)
   ADR 0011 reference + Phase 5 skip marker (T71)
   [no checkpoint — phase is intentional no-op]
        ↓
Spec 05 Phase 6: chrome mounting in fast
   delete fast's local AppShell/Sidebar/Header (T72)
   mount React chrome from @coms-portal/ui-react (T73)
   wire data.appCatalog from /api/userinfo (T74)
   brand the chrome + visual parity check (T75)
        ↓
   CHECKPOINT 17: fast renders the platform chrome
        ↓
Spec 05 Phase 7: app registry + webhooks
   rerun register-fast script with post-Phase-4 URLs (T76)
   author webhook consumer (T77)
   taxonomy projection (T78)
   health check endpoint (T79)
        ↓
   CHECKPOINT 18: fast in app_registry; webhooks deliver
        ↓
Spec 05 Phase 8: per-service IaC + deploy workflows
   author infra/fast/ mirroring infra/heroes/ (T80)
   author deploy-fast.yml workflow (T81)
   register fast WIF deployer SA (T82)
   first IaC apply window (T83)
        ↓
   CHECKPOINT 19: coms-fast-web Cloud Run service live
        ↓
Spec 05 Phase 9: PWA installability
   author manifest.webmanifest at /fast/ scope (T84)
   author service-worker.js mirroring portal-web pattern (T85)
   Lighthouse PWA audit + on-device install verify (T86)
        ↓
   CHECKPOINT 20: fast installable as distinct PWA
        ↓
Spec 05 Phase 10: verification + documentation
   E2E smoke (sign-in cross-app + chrome + admin + public surfaces + OAuth) (T87)
   performance check (p50/p95 flat-or-faster vs pre-migration) (T88)
   author apps/fast/README.md as React-side reference (T89)
   update docs/integration-contract.md to reference fast alongside heroes (T90)
        ↓
   CHECKPOINT 21: Spec 05 complete — fast is the React-side reference
```

## Vertical slicing approach

Each task in `tasks/todo.md` is sliced **vertically** — it delivers a complete working state, not a partial step. Specifically:

- A "subtree-merge a lib" task includes the subtree + workspace registration + dependency conversion + verification, end-to-end, in one task. Not "step 1 of N."
- A "JWT replacement" task includes the new function + replacing all consumers + the drop migration + verification, all the way to "heroes auths via JWT and has no session table."

**Anti-pattern:** Don't split horizontally ("do all subtree merges first, then convert all dependencies, then verify"). Horizontal slicing creates broken intermediate states where the working tree doesn't compile.

## Checkpoints

Each `CHECKPOINT N` in the graph is a **hard gate**. Before crossing:

- `bun install --frozen-lockfile` at the monorepo root must succeed.
- `bun run typecheck` (or per-package equivalent) must pass for affected packages.
- `bun run test` must pass for packages with tests.
- Any checkpoint-specific verification listed in `todo.md` must succeed.

**If a checkpoint fails: stop. Fix. Re-verify. Don't proceed past red.**

A failed checkpoint usually means the previous phase wasn't truly vertically sliced — go back and finish it before moving on.

## Session-handoff protocol

When a fresh Claude session picks up this plan:

1. Read `tasks/plan.md` (this file) and `tasks/todo.md`.
2. Find the next unchecked task in `todo.md`.
3. Read the relevant Spec section in `docs/spec/` for full context.
4. Read relevant ADRs (the task description will reference them).
5. Mark the task `[~]` (in progress) before starting.
6. Execute the task and run its verification.
7. Mark the task `[x]` (complete) when verified — never on intent alone.
8. Commit status changes alongside the work (one logical commit per task or related task cluster, Mr. Door voice).
9. If blocked: leave the task `[~]`, append a `Blocker:` line under the task in `todo.md`, surface to the user.

**Both files are git-tracked.** Status updates go in commits.

## Risks worth tracking

| Risk | Phase | Likelihood | Mitigation |
|---|---|---|---|
| SDK `0.1.1` → current gap in `apps/api` hides breaking changes | Phase 1 (T08) | High | Surface immediately. Reserve a half-day for porting whatever diverged between issuer-side and verifier-side SDK code. |
| Heroes' base-path migration breaks scattered absolute links | Phase 1 Spec 02 (T25) | High | Audit `grep -rn 'href="/[a-z]'` thoroughly. Run heroes against base-path-prefixed staging URL before any production cutover. |
| `bun install` quirks on native deps (sharp, lightningcss, Prisma) | Spec 05 Phase 2 (T58) | Medium | Survey fast's deps before merge; identify any native-binary deps; spike bun install against a copy of fast's package.json before the workspace merge. |
| Heroes' app-side glue won't fully absorb into chrome lib | Phase 4 Spec 02 (T40-T43) | Medium | Some patterns may stay app-local. Document the judgment calls when they arise. |
| Webhook consumer relied on session staleness | Phase 2 Spec 02 (T36) | Low | Audit `portal_webhook_events` consumers for assumptions about session lifetime before drop-table migration. |
| Heroes freeze window not coordinated | Phase 3 Spec 01 (T10) | Medium | T10 is explicitly a coordination task; do not skip. |
| Firebase Hosting passthrough quirks (cookies, streaming SSR) | Phase 5 Spec 01 (T19-T20) | Medium | Pre-spike with one app behind Firebase Hosting before committing the architecture. |
| React chrome port produces visual or behavioural divergence from Svelte parity | Spec 05 Phase 1 (T50–T54) | High | Pixel-level comparison at every breakpoint + theme. The Svelte version is the authority; React mirrors. Stub consumer (T53) validates prop shapes before fast adopts in Phase 6. |
| Better Auth removal misses a `requireAuth()` call site → 401 in prod | Spec 05 Phase 3 (T61) | High | 137 grep hits across `app/lib/components` quantify the surface. Sub-phase (b) is verified by re-grep returning zero matches before sub-phase (c) drops the implementation. Three-stage cadence (ADR 0011 §4 Option B) makes any miss fail in (b) not after (c). |
| Next.js `basePath: '/fast'` breaks scattered absolute paths | Spec 05 Phase 4 (T66) | High | Audit pattern proven on heroes T25 + portal FU-10. Run `grep -rn 'href="/[a-z]'` + `grep -rn 'fetch("/[a-z]'` + `grep -rn "router.push('/'" ` before flipping basePath. |
| Google Calendar OAuth redirect URI mismatch after base-path change | Spec 05 Phase 4 (T67) | Medium | Register the new `/fast/` redirect URI in Google Cloud Console BEFORE the deploy that flips basePath. Old URI stays registered through one rollback window. |
| Prisma `User` PK shape change (cuid → portal-UUID) breaks 30+ relations | Spec 05 Phase 3 (T64) | High | Staged migration: `portal_sub` column adds nullable first, backfills via email_normalized join against portal's identity_users, code flips to read it, schema migration constrains it only after every active user has a value. Mirrors heroes' `can_submit_points` shape. |
| Two-PWA-per-origin scope collision (already mitigated by FU-10) extends to three | Spec 05 Phase 9 (T84) | Low | FU-10 closed the structural issue at the manifest level — portal/`portal`, heroes/`/heroes/`, and fast/`/fast/` are scope-disjoint. Verify start_url + scope + id all `/fast/`; Lighthouse audit catches scope misconfiguration. |
| 30+ alifm17 product surfaces introduce things this spec didn't audit | Spec 05 all phases | Medium | Re-audit after T56 subtree merge. PROJECT_CONTEXT.md last updated 2026-03-31; the tree shape evolved. T59's verification step is the first signal something broke. |
| Webhook consumer's idempotency fails under retry storms during cutover | Spec 05 Phase 7 (T77) | Low | `portal_webhook_events` dedup table follows heroes' pattern. Pre-deploy: confirm table exists + unique-key constraint enforced. |

## Standing principles for IaC across all apps

These predate the current plan and outlive it — every future app onboarding (aha-fast, app 3, app 4) follows the same shape unless an explicit ADR justifies an exception.

1. **Per-app resources by default.** Each app owns its own Cloud Run services, Artifact Registry repo, runtime SAs (one per service), monitoring filters, secrets, and Cloud Build pipelines. Shared resources need an explicit reason recorded in an ADR — not a copy-paste from another app's template. The blast radius of a bad change in app A must not reach app B; the migration path of spinning out an app must not require extracting its data from a shared store. **Cloud SQL instances are per-app unless explicitly recorded otherwise.** Portal + heroes share `coms-aha-heroes-db` (`34.50.119.90`, asia-southeast2) — a historical artefact predating this principle, retained because the operational cost of splitting outweighs the principle benefit today. Fast carries its own instance `aha-fast-db-instance-cd5db712` (`34.101.176.36`, asia-southeast2), the result of fast's pre-merge polyrepo provisioning that accidentally aligns with the principle. The naming wart (`aha-fast-` prefix instead of `coms-fast-` per principle 2) is operationally costly to fix in-place (Cloud SQL doesn't support rename; recreate + DMS migration) and stays as-is. Project, VPC, WIF pool remain shared by suite-wide-resource design; future apps adopting this monorepo get their own Cloud SQL instance by default.
2. **Naming convention: `coms-<app>-<resource>`.** New IaC drops the `aha` infix — heroes' new resources are `coms-heroes-api`, `coms-heroes-web`, `coms-heroes-repo`, `coms-heroes-{api,web}-sa`. Existing `coms-aha-*` names that are operationally costly to rename (GCS buckets holding data, the Tofu state bucket itself, the sheet-sync SA whose email is shared with live Google Sheets) stay as they are; cheap renames (the Artifact Registry repo, dead WIF resources) get aligned as the surrounding work touches them.
3. **App-side Tofu state stays self-contained.** An app's Tofu state should not have a hard dependency on another app's state by data lookup unless the dependency is genuinely cross-cutting (e.g. an org-wide WIF pool). Cross-state lookups bind apply ordering and complicate the spin-out story; prefer duplicating a small constant over linking states.
4. **Every cost-driving GCP resource carries the standard label set.** Allocation policy gives the suite one shared GCP project (`fbi-dev-484410`); labels are the only first-class mechanism for per-app cost attribution and audit. Every Tofu-managed resource that supports labels carries:

   ```
   app         = "portal" | "heroes" | "fast" | "<future>"   # the product unit
   service     = "<app>-api" | "<app>-web" | …               # only on per-service resources
   environment = "prod"                                       # forward-compat: "staging" | "preview"
   managed-by  = "opentofu"                                   # distinguishes IaC from console-created
   ```

   Per-state `locals.<app>_labels` block holds the shared labels; per-service overrides extend it. Resources GCP doesn't support labels on (service accounts, WIF pools/providers, IAM bindings, Cloud Tasks queues, Cloud Scheduler jobs) carry the attribution via the `coms-<app>-<resource>` naming convention from principle 2. Cost rollups: filter Cloud Billing reports by `label:app` for the headline number; drill into `label:service` for intra-app breakdown. Budget alerts and BigQuery billing export are the proactive complements — set them up alongside any new app onboarding.

5. **CI/CD via GitHub Actions while the repo stays public; `tofu apply` stays on the laptop.** Deploys run in GHA: `mrdoorba/aha-coms` is a public GitHub repo, which means unlimited free Actions minutes for standard runners. Cloud Build with `E2_HIGHCPU_8` (the machine type the cloudbuild yamls request) has no free tier — every minute is paid. T17 returned all four service deploys to GHA workflows (`deploy-{portal,heroes}-{api,web}.yml`) authenticating to GCP via WIF. Each app keeps its own deployer SA + WIF pool (per-app principle): portal via `coms-portal-github-actions`, heroes via `coms-heroes-deployer-sa`. The cloudbuild yamls in `apps/*/cloudbuild.yaml` remain in the tree as a manual escape hatch — `gcloud builds submit --config apps/<service>/cloudbuild.yaml .` still works for one-off deploys when the workflow is offline. **Tofu apply is deliberately NOT in GHA** — auto-apply has no clean rollback for Cloud SQL recreation, IAM rotation, or secrets deletion, and workflow_dispatch adds ceremony without material safety wins for a two-operator team. The supported laptop-CLI runbook lives at `infra/README.md`. **DB migrations run inside each API's deploy workflow** — portal-api and heroes-api both carry an `Apply DB migrations` step that downloads `cloud-sql-proxy`, fetches the URL secret, rewrites the host segment from Cloud Run's Unix-socket DSN to the proxy port, and runs `bun --filter @coms-portal/<svc>-api db:migrate` BEFORE `docker build` (FU-3 wired portal; FU-5 wired heroes after Phase 5's two migrations made the manual ritual the third occurrence in seven days). Drizzle migrations in this project are append-only nullable adds / non-destructive UPDATEs, which makes automating them safe; destructive migrations (T46's `DROP TABLE user_config_cache` is the canonical exception) need the deploy-first-then-migrate ordering that this workflow's migrate-first-then-deploy shape doesn't cover — both workflow comments name the constraint so a future implementer authoring a `DROP COLUMN` stops at the source. The manual orchestration gap that bit FU-1 (portal) and the Phase 5 cutover (heroes) stays closed by the workflow rather than by operator memory. Reconsider these decisions if (a) the repo turns private, or (b) an app needs a build path Cloud Build does materially better (custom workers, GCS-private base images, etc.), or (c) the team grows past three operators / incident rate makes the audit gap matter (reopens FU-4 from `tasks/todo.md`'s "Future upgrade path"). Per-app exceptions get recorded in an ADR.

## What's deliberately not in this plan

- **aha-fast onboarding** — Spec 05 authored 2026-05-13 at [`docs/spec/05-fast-onboarding.md`](../docs/spec/05-fast-onboarding.md). **Now in execution as of 2026-05-13**: CP11 crossed, FU-9 deferred (heroes-side runtime verification, contract-orthogonal), FU-10 source + deploy landed (portal-web moved to `/portal/` so fast at `/fast/` no longer collides with portal's manifest scope). All four open questions resolved on the spec's recommendations: §1 keep Prisma (ADR 0011 records the per-app exception with reopen criteria), §2 fold React chrome port into Phase 1, §3 unified `apps/fast/`, §4 stage Better Auth removal in three sub-phases. Phase 1 tasks (T50–T54: audit Svelte chrome surface → port to React → port Sheet primitive → stub consumer → visual parity check) seeded in `tasks/todo.md`. Phase 2+ tasks author as each near-term phase opens. Ten phases / ten checkpoints (CP12–CP21) total; Phase 1 is the load-bearing prerequisite (~1–2 weeks of careful porting work).
- **Spec 03 (Integration Test Kit)** — stub exists at `docs/spec/03-integration-test-kit.md`. Picked up after Spec 02 lands.
- **Spec 04 (SDK as Enforcement Layer)** — stub at `docs/spec/04-sdk-as-enforcement-layer.md`. Picked up alongside or after Spec 03.
- **Platform-owned notifications** (the one contract deviation in heroes). Separate future spec.
- **Apps 3 and 4 onboarding.** Future work; the integration contract is the spec they integrate against.

## Confidence in the plan

| Aspect | Confidence | Notes |
|---|---|---|
| Phase ordering (Spec 01 + 02) | High | Dependency graph reflects real dependencies; proven by Spec 01 + Spec 02 executing through to CP11 without reordering. |
| Phase ordering (Spec 05) | High | Mirrors Spec 02's vertical-slice cadence; Open Question §1–§4 decisions resolved + recorded before phase tasks landed. |
| Vertical slicing | High | Each task delivers a working state; the Phase 3 sub-phase shape (T60→T64) makes the Better Auth removal incrementally verifiable rather than atomic. |
| Acceptance criteria | Medium | Concrete file paths + grep patterns + verification commands named. Some Phase 6/7 specifics (fast's brand mark, taxonomy projection scope) sharpen when execution hits the relevant code. |
| Effort estimates | Not provided | Deliberately. The user estimates as tasks are picked up. Phase 1 (chrome port) is signalled as the heaviest single body; Phase 3 (Better Auth removal, 127 call sites) is second. |
| Coordination assumptions | Low | "Fast freeze window" (T55) mirrors heroes T10's shape; the operator confirmed initiation before T55 opens. |
| Spec 05 codebase audit | High | aha-fast surveyed 2026-05-13: 127 API routes, 137 grep hits on `requireAuth\|getServerSession\|useAuth`, 42 Prisma models (4 auth-shaped), 5 credential routes. The task acceptance criteria reference these specific numbers. |

Adjustments to this plan are expected as execution surfaces information. The plan is a living document; update it alongside `todo.md` as decisions evolve.
