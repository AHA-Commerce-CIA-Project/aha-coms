## Summary

<!-- One or two lines on what this PR does and why. -->

## What does this touch?

- [ ] `apps/fast/` only — change is local to fast
- [ ] `packages/*` — shared with portal + heroes; cross-app impact possible
- [ ] `infra/*` — IaC changes; affects production resources
- [ ] `.github/workflows/*` — CI changes

## Cross-app impact (only if you ticked `packages/*` or `infra/*`)

- [ ] Ran `bun run --filter '*' typecheck` and every package exits 0
- [ ] N/A — change is fast-only

## Test plan

<!-- How did you verify this works? Commands run, browsers tested, manual flows checked. -->

## Confidence + scope-risk

<!-- High / medium / low confidence. What breaks if this is wrong? -->
